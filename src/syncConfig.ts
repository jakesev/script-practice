// Filled in once your free Supabase project exists (Settings → API → Project URL + anon public key).
// Safe to commit: the anon key is public by design, and the database only exposes the
// sync_pull / sync_push functions, which are gated by your secret sync code (see supabase/sync.sql).
export const SUPABASE_URL = ''
export const SUPABASE_ANON_KEY = ''
